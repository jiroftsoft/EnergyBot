# 📦 فهرست فایل‌های مورد نیاز برای آپلود

## 🎯 ساختار پیشنهادی برای هاست ویندوزی

```
httpdocs/                    (سایت ASP.NET شما - دست نزنید)
energybot/                   (پوشه جدید برای ربات - اینجا را بسازید)
├── app/                     (کل پوشه app)
│   ├── core/
│   ├── db/
│   ├── services/
│   ├── telegram/
│   └── web_admin/
├── logs/                    (پوشه برای لاگ‌ها)
├── requirements.txt
├── .env                     (بعد از آپلود، این فایل را ویرایش کنید)
└── README.md
```

---

## ✅ فایل‌هایی که باید آپلود کنید

### 1. پوشه `app/` (کل پوشه)
```
app/
├── __init__.py
├── core/
│   ├── __init__.py
│   ├── config.py
│   └── logging_config.py
├── db/
│   ├── __init__.py
│   ├── base.py
│   ├── session.py
│   ├── models.py
│   └── seed.py
├── services/
│   ├── __init__.py
│   ├── subscriptions.py
│   ├── payments/
│   │   ├── __init__.py
│   │   └── zarinpal.py
│   └── ai/
│       ├── __init__.py
│       └── plans.py
├── telegram/
│   ├── __init__.py
│   ├── main.py
│   ├── handlers/
│   ├── keyboards/
│   ├── states/
│   └── utils/
└── web_admin/
    ├── __init__.py
    ├── main.py
    ├── routes/
    └── templates/
```

### 2. فایل‌های ریشه پروژه
```
requirements.txt
README.md
.env.example
.gitignore
```

### 3. فایل‌های deployment (برای بعد)
```
deploy.sh
supervisor.conf
nginx.conf
backup.sh
```

### 4. فایل‌های راهنما (اختیاری)
```
QUICK_START.md
DEPLOYMENT_README.md
PLESK_SETUP.md
```

---

## 📝 مراحل آپلود

### مرحله 1: ساخت پوشه جدید

در Plesk File Manager:
1. به `httpdocs` بروید
2. یک پوشه جدید با نام `energybot` بسازید
3. یا می‌توانید در root هاست (هم‌سطح `httpdocs`) بسازید

**توصیه:** در root هاست بسازید (هم‌سطح `httpdocs`):
```
/
├── httpdocs/          (سایت ASP.NET شما)
└── energybot/         (پوشه جدید برای ربات)
```

### مرحله 2: آپلود فایل‌ها

**روش 1: از طریق Plesk File Manager**
1. به پوشه `energybot` بروید
2. تمام فایل‌ها و پوشه‌ها را آپلود کنید
3. ساختار پوشه‌ها را حفظ کنید

**روش 2: از طریق FTP**
- از FileZilla یا هر FTP client استفاده کنید
- به پوشه `energybot` متصل شوید
- فایل‌ها را آپلود کنید

**روش 3: از طریق SSH (اگر دسترسی دارید)**
```bash
# در سرور
cd /path/to/energybot
# سپس فایل‌ها را از کامپیوتر محلی آپلود کنید
```

---

## ⚠️ نکات مهم برای هاست ویندوزی

1. **Python باید نصب باشد:**
   - در هاست ویندوزی، Python ممکن است نصب نباشد
   - باید Python 3.11+ نصب کنید

2. **مسیر Python:**
   - در Windows معمولاً: `C:\Python311\python.exe`
   - یا: `C:\Program Files\Python311\python.exe`

3. **Virtual Environment:**
   - در Windows: `python -m venv venv`
   - فعال‌سازی: `venv\Scripts\activate`

4. **فایل .env:**
   - بعد از آپلود، فایل `.env` را ویرایش کنید
   - مقادیر واقعی را وارد کنید

---

## 🔧 بعد از آپلود

1. فایل `.env` را ویرایش کنید
2. Python را نصب کنید (اگر نصب نیست)
3. Virtual Environment بسازید
4. وابستگی‌ها را نصب کنید
5. به من اطلاع دهید تا ادامه دهم

---

## 📋 چک‌لیست آپلود

- [ ] پوشه `energybot` ساخته شد
- [ ] پوشه `app/` کامل آپلود شد
- [ ] فایل `requirements.txt` آپلود شد
- [ ] فایل `.env.example` آپلود شد (بعداً به `.env` تبدیل می‌شود)
- [ ] پوشه `logs/` ساخته شد (خالی)
- [ ] ساختار پوشه‌ها درست است

---

**بعد از آپلود، به من اطلاع دهید تا ادامه دهم! 🚀**

