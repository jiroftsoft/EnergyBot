# 📤 راهنمای آپلود فایل‌ها برای هاست ویندوزی

## 🎯 خلاصه: چه فایل‌هایی را آپلود کنم؟

### ✅ باید آپلود کنید:

1. **کل پوشه `app/`** (با تمام زیرپوشه‌ها)
2. **فایل `requirements.txt`**
3. **فایل `.env.example`** (بعداً به `.env` تبدیل می‌شود)
4. **فایل `install.ps1`** (برای نصب خودکار)
5. **فایل‌های راهنما** (اختیاری)

### ❌ نیازی به آپلود نیست:

- `.gitignore`
- `README.md`
- فایل‌های `.pyc` و `__pycache__`
- پوشه `venv/` (در سرور ساخته می‌شود)
- فایل `.env` (بعد از آپلود ساخته می‌شود)
- فایل `gym_bot.db` (بعد از اجرا ساخته می‌شود)

---

## 📁 ساختار پوشه در هاست

```
C:\inetpub\vhosts\mehranyad.ir\        (یا مسیر هاست شما)
└── energybot\                          (پوشه جدید - اینجا را بسازید)
    ├── app\                            ✅ آپلود کنید
    │   ├── core\
    │   ├── db\
    │   ├── services\
    │   ├── telegram\
    │   └── web_admin\
    ├── logs\                           (پوشه خالی - بسازید)
    ├── requirements.txt                ✅ آپلود کنید
    ├── .env.example                    ✅ آپلود کنید
    └── install.ps1                      ✅ آپلود کنید
```

---

## 📝 مراحل آپلود

### مرحله 1: ساخت پوشه

**از طریق Plesk File Manager:**
1. به root هاست بروید (هم‌سطح `httpdocs`)
2. **New Folder** را بزنید
3. نام: `energybot`
4. پوشه را بسازید

### مرحله 2: آپلود فایل‌ها

**روش 1: Plesk File Manager**
1. به پوشه `energybot` بروید
2. **Upload Files** را بزنید
3. فایل‌ها را انتخاب و آپلود کنید
4. **ساختار پوشه‌ها را حفظ کنید**

**روش 2: FTP (FileZilla)**
1. به سرور متصل شوید
2. به پوشه `energybot` بروید
3. فایل‌ها را drag & drop کنید

---

## ✅ چک‌لیست بعد از آپلود

بعد از آپلود، بررسی کنید:

- [ ] پوشه `energybot` ساخته شد
- [ ] پوشه `app/` کامل آپلود شد (با تمام زیرپوشه‌ها)
- [ ] فایل `requirements.txt` آپلود شد
- [ ] فایل `.env.example` آپلود شد
- [ ] فایل `install.ps1` آپلود شد
- [ ] پوشه `logs/` ساخته شد (خالی)
- [ ] ساختار پوشه‌ها درست است

---

## 🔧 بعد از آپلود

1. **فایل `.env` را بسازید:**
   - `.env.example` را کپی کنید
   - نامش را به `.env` تغییر دهید
   - مقادیر واقعی را وارد کنید

2. **اسکریپت نصب را اجرا کنید:**
   - از طریق SSH به سرور متصل شوید
   - به پوشه `energybot` بروید
   - دستور زیر را اجرا کنید:
   ```powershell
   .\install.ps1
   ```

3. **به من اطلاع دهید:**
   - بعد از آپلود و اجرای `install.ps1`
   - به من بگویید تا ادامه دهم

---

## 📚 فایل‌های راهنما

- **UPLOAD_LIST.md** - فهرست کامل فایل‌ها
- **WINDOWS_HOST_SETUP.md** - راهنمای نصب روی Windows
- **QUICK_START.md** - راهنمای سریع

---

## ⚠️ نکات مهم

1. **ساختار پوشه‌ها:** حتماً ساختار را حفظ کنید
2. **فایل .env:** هرگز در Git commit نکنید
3. **Python:** باید Python 3.11+ در سرور نصب باشد
4. **SSL:** باید در Plesk فعال باشد

---

**بعد از آپلود، به من اطلاع دهید! 🚀**

