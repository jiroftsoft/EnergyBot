# ✅ راه‌حل نهایی: بدون نیاز به Python در هاست

## 🎯 مشکل

هاست شما (سرورپارس) از Python پشتیبانی نمی‌کند.

## 💡 راه‌حل

1. **ربات را روی Cloud (Railway/Render) اجرا می‌کنیم** (رایگان)
2. **یک صفحه ساده در `mehranyad.ir` می‌سازیم** که callback را forward می‌کند

---

## 📋 مراحل ساده

### مرحله 1: Deploy روی Railway (5 دقیقه)

1. کد را به GitHub push کنید
2. در [railway.app](https://railway.app) ثبت‌نام کنید
3. Repository را connect کنید
4. Environment Variables را تنظیم کنید
5. Railway خودکار deploy می‌کند

**راهنمای کامل:** `RAILWAY_DEPLOY.md`

---

### مرحله 2: آپلود فایل Callback در هاست (2 دقیقه)

**گزینه A: اگر سایت شما PHP دارد:**

1. فایل `httpdocs/zarinpal_callback.php` را در `httpdocs` آپلود کنید
2. URL سرور Python را در فایل به‌روزرسانی کنید:
   ```php
   $pythonServerUrl = 'https://YOUR-RAILWAY-URL.railway.app';
   ```

**گزینه B: اگر سایت شما ASP.NET دارد:**

1. فایل `httpdocs/zarinpal_callback.aspx` را در `httpdocs` آپلود کنید
2. URL سرور Python را در فایل به‌روزرسانی کنید:
   ```csharp
   string pythonServerUrl = "https://YOUR-RAILWAY-URL.railway.app";
   ```

---

### مرحله 3: تنظیم در زرین‌پال (1 دقیقه)

1. وارد پنل زرین‌پال شوید
2. Callback URL را تنظیم کنید:
   ```
   https://mehranyad.ir/zarinpal_callback.php
   ```
   یا
   ```
   https://mehranyad.ir/zarinpal_callback.aspx
   ```

---

## ✅ نتیجه

- ✅ ربات روی Railway اجرا می‌شود (رایگان)
- ✅ Callback URL از دامین شما (`mehranyad.ir`) استفاده می‌کند
- ✅ هیچ تغییری در هاست شما نیاز نیست
- ✅ سایت ASP.NET شما دست نخورده می‌ماند

---

## 📁 فایل‌های آماده

1. **`httpdocs/zarinpal_callback.php`** - برای PHP
2. **`httpdocs/zarinpal_callback.aspx`** - برای ASP.NET
3. **`RAILWAY_DEPLOY.md`** - راهنمای کامل Railway
4. **`SOLUTION_CLOUD.md`** - توضیحات بیشتر

---

## 🚀 شروع کنید

1. **ابتدا:** `RAILWAY_DEPLOY.md` را بخوانید و ربات را deploy کنید
2. **بعد:** فایل callback را در هاست آپلود کنید
3. **در آخر:** Callback URL را در زرین‌پال تنظیم کنید

---

## ❓ سوالات متداول

### آیا Railway رایگان است؟
بله، تا 500 ساعت در ماه رایگان است.

### آیا دامین من در callback استفاده می‌شود؟
بله، زرین‌پال به `mehranyad.ir` redirect می‌کند و از آنجا به Railway forward می‌شود.

### آیا سایت ASP.NET من تغییر می‌کند؟
خیر، فقط یک فایل PHP/ASP.NET اضافه می‌شود که callback را forward می‌کند.

### آیا می‌توانم از Render استفاده کنم؟
بله، راهنمای مشابهی در `SOLUTION_CLOUD.md` است.

---

**موفق باشید! 🎉**

